import React from 'react'
const lastName = "SIMPSON"

const avatar = [{FirstName: "Homer", lastName, avatar:"" }, {FirstName: "Bart", lastName, avatar:"" }]

const Avatar = ({name, avatar}) => {
  return (
    <div>
        <img src="#" />
        <h3>{FirstName}</h3>
    </div>
    )
}

export default Avatar